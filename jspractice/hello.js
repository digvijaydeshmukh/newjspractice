let student = [
    { name: "Piyush", rollNumber: 31, marks: 80 },
    { name: "Jenny", rollNumber: 15, marks: 69 },
    { name: "Ram", rollNumber: 16, marks: 35 },
    { name: "Ashish", rollNumber: 7, marks: 55 }
];

// let names=[];

// for(let i=0;i<student.length;i++){
//     names.push(student[i].name.toUpperCase())
// }
// let names=student.map(el=>el.name.toUpperCase())
// let temp=student.filter(el=>el.marks>60).map(el=>el.name)
// let temp=student.filter(el=>el.marks>60 && el.rollNumber>15).map(el=>el.name)
// let summarksAll=student.reduce((acc,c)=>{return acc+c.marks},0)
// console.log(summarksAll)

// let temp=student.map(el=>{
//     if(el.marks<60){
//         el.marks+=20
//     }
//     return el
// }).filter(el=>el.marks>60).reduce((acc,c)=>acc+c.marks,0)

// console.log(temp)